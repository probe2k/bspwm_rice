set icons true
set drawbox true
set preview true

cmd open ${{
	case $(file --mime-type "$f" -bL) in
		text/*|application/json) v "$f" ;;
		*) setsid -f xdg-open "$f" ;;
	esac
}}

map D delete
map A rename
map <enter> shell
